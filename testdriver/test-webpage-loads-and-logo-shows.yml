version: 4.0.79
steps:
  - prompt: Wait for the webpage to load.
    commands:
      - command: wait-for-text
        text: The complete real-time developer platform
  - prompt: >-
      Assert that the PubNub logo is visible on the homepage.  # Use the content
      of the file from the environment
    commands:
      - command: assert
        expect: the PubNub logo is visible
